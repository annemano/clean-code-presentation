name: Go build and test CLI TEST

on:
  workflow_dispatch:
    inputs:
      tag:
        description: 'Add new version tag to commit'
        type: choice
        options:
          - (git rev-parse HEAD)
          - Minor
          - Patch
        required: true

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: '1.22.x'

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    steps:
      - name: "Testing manual workflow"
        run: echo "Currently testing manual workflow..."
